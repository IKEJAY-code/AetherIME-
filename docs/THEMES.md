# Theme Notes

当前仓库提供两个主题 token 文件：

- `themes/deep-ocean/theme.toml`
- `themes/mist-light/theme.toml`

用途：

- 统一候选窗和幽灵文字配色
- 后续映射到 Fcitx5 UI 参数或自定义渲染层

建议：

- 深色环境默认 `deep-ocean`
- 浅色环境默认 `mist-light`
- 幽灵文字 alpha 保持 `0.5 ~ 0.65` 避免影响主输入可读性
